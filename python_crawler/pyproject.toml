[project]
name = "amazon-crawler"
version = "0.1.0"
description = "Amazon crawler using Playwright - Best Sellers & Keyword Search"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.1",
    "playwright>=1.58.0",
]

[project.scripts]
crawl = "main:main"
dashboard = "dashboard:main"
